"""Helpers to use the TCL files, either by incorporating them into a project or by executing them for specific tasks."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
